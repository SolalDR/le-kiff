import {c} from "~/helpers/Configuration";

export default c({
  fill: {
    color: new THREE.Color(0x000000),
    opacity: 0.2
  },
  stroke: {
    color: new THREE.Color(0xFFFFFF),
    fillWidth: 0.01
  }
})
