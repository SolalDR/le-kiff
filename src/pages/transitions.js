import { TimelineLite } from 'gsap';

export const enterStatic = (staticEl) => {
  const timeline = new TimelineLite();
  const ui = {
  }

  staticEl.classList.add('is-shown');
}


export const exitStatic = (staticEl) => {
  const ui = {
  }

}
